= Vert.x **J**son **P**ersistence simil**A**r to JPA


This provides an asynchronous interface around a JDBC datasource to:

- delete
- merge
- persist
- query with named parameters
- query with dynamic restrictions handler


 in a simple way.

